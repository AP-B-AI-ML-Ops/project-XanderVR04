apiVersion: 1

datasources:
  - name: PostgreSQL
    uid: postgres-metrics
    type: postgres
    url: database:5432
    user: ${POSTGRES_USER}
    secureJsonData:
      password: ${POSTGRES_PASSWORD}
    jsonData:
      database: metrics
      sslmode: disable
      postgresVersion: 1600
    isDefault: true
